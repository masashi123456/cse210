public class Bookcase
{
    private List<Book>_books = new List<Book>();
      public void AddBook(Book book)
      {
        _books.Add(book);
      }

      public void ShowBook()
      {
        
      }
      public void CountBookds()
      {
        Console.WriteLine($"")
      }
    }
}